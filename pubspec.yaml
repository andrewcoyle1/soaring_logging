name: soaring_logging
description: A plugin-based logging framework for Flutter/Dart applications. Provides a unified interface for integrating multiple logging services.
version: 1.0.0
repository: https://github.com/andrewcoyle1/soaring_logging

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
